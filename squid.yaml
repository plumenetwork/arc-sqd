manifestVersion: subsquid.io/v0.1
name: arc-sqd
version: 1
description: 'The sqd for Arc.'
build: 
deploy:
  addons:
    postgres: 
    rpc:
      - eth.http
  processor:
    cmd:
      - sqd
      - process:prod
    env:
      RPC_ETH_HTTP: https://testnet-rpc.plumenetwork.xyz/http
  api:
    cmd:
      - sqd
      - serve:prod
